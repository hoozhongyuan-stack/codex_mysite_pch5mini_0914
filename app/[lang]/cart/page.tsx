import {notFound} from 'next/navigation';import {PublicShell} from '@/lib/public-view';import {publicExtras,navLinks} from '@/lib/public-cms';import {siteSettings,published} from '@/lib/server';import ShopClient from '@/app/shop-client';
export const dynamic='force-dynamic';export const metadata={title:'购物车 / Cart',robots:{index:false,follow:false}};
export default async function Page({params}:{params:Promise<{lang:string}>}){const {lang}=await params;if(!['zh','en'].includes(lang))notFound();const [settings,extra,all]=await Promise.all([siteSettings(),publicExtras(),published()]);return <PublicShell lang={lang} settings={settings} theme={settings.theme} path={['cart']} links={navLinks(extra,all,lang)} policies={extra.policies}><ShopClient lang={lang} kind="cart"/></PublicShell>}
