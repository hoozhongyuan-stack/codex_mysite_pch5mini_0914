# 项目协作约定

本文件补充上级协作约定，不替代用户当前指令、既有安全规则或测试要求。

## 产品需求文档

- 本项目需求基线为 [`docs/产品需求文档.md`](docs/产品需求文档.md)。开始需求开发前先阅读涉及的章节。
- 用户已要求持续维护该文档。新增或修改需求经用户确认后，同步编号、规则、验收要点、状态及变更历史；未确认的建议只能标记“待确认”。
- 每次需求开发完成，在交付前更新对应条目、验证证据和未完成事项，并维护文档版本和日期。
- 最新明确用户指令优先于旧文档；发生修订时记录取代关系，不悄悄删除历史决策。
- 区分“本地已实现”“沙箱已实现”“待真实接入”“待验收”“已验收”。构建或接口测试通过不代表浏览器、真实第三方授权或生产验收通过。
- 当前工作默认本地开发与验收，生产部署需用户明确授权。
- 文档、日志及需求记录禁止写入密码、SMTP 授权码、第三方应用密钥、令牌或真实访客隐私资料。


## 本项目多代理策略

本节覆盖上级 ECC 中要求自动启动代理的流程规定。

- 默认由主代理完成规划、实现、测试和代码自审。
- 不因普通修改自动启动 planner、tdd-guide 或 code-reviewer。
- 仅在用户明确要求，或涉及鉴权、支付、敏感数据、
  数据库迁移等高风险修改时，使用必要的专项代理。
- 其他复杂任务只有在存在明确、可独立开展的子任务时才委派，
  默认最多增加一个子代理。
- 保留现有测试、安全、需求文档和验收要求。